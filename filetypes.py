# import subprocess

# try:
#     exiftool = not subprocess.call(['which', 'exiftool'])
# except WindowsError:
#     exiftool = None

from filetypes_fallback import *
# ditched the exiftool plan for now.
